[package]
name = "devos-cli"
version = "1.0.0"
edition = "2021"
description = "CLI tool for controlling DevOS"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
